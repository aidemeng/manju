# Manju Skill 前向测试记录

## 测试范围

测试对象：`skills/manju-skill/`

测试均由独立评估者执行，产物写入 `/tmp`，未修改仓库文件。测试关注模式选择、按需加载、输出边界、上游保护、评分门槛和安全约束。

## 1. Outline 模式

**请求：** 创建一部面向成年观众的都市奇幻漫剧：一名28岁的文物修复师能听见古物记忆，请先完成项目设定和80集大纲。

**结果：PASS**

- 正确选择 `outline`。
- 只读取故事开发、项目模板和大纲模板；未加载分镜或质检参考。
- 创建 `project.md` 和 `outline.md`。
- 大纲恰好80集，编号1–80连续。
- 五幕范围分别为1–16、17–32、33–48、49–64、65–80。
- 无模板占位符残留。
- 所有情感参与者明确成年，未发现胁迫亲密或未成年编码。

产物目录：`/tmp/manju-forward-outline/`

## 2. Write 模式

**请求：** 在具有项目、大纲、角色、目录和第10集的最小已有项目中续写第11–15集。

**结果：PASS**

- 正确选择 `write`，完成后执行一次批次 `review`。
- 只新建 `episode-011.md` 至 `episode-015.md`。
- 项目、大纲、角色、目录和第10集在执行前后的哈希一致。
- 五集均只使用一种五列表格分镜格式，并包含连续性锁定。
- 时间轴连续覆盖0–120秒，无重叠和空档。
- 批次质检得分95/100，通过。

产物目录：`/tmp/manju-forward-write/`

### Write 回归测试

**目标：** 检查 supplied-range 边界没有下一集目录行时的行为，以及制作成本统计口径。

**结果：PASS**

- 缺失第16集目录行被记录为非阻断盲区。
- 第15集依据已锁定的上游结尾问题正常生成，未创建第16集。
- 质检使用剧集级视觉权重，只按制作备注列出高成本镜头，没有臆造镜头级权重。
- 机械验证输出：`REGRESSION_PASS missing_next_row=blind_spot_non_blocking visual_stats=episode_level_plus_noted_costly_shots no_episode_016=1 files=7`。

产物目录：`/tmp/manju-forward-write-v2/`

## 3. Review 模式

**请求：** 质检一个包含两个相邻重复结尾钩子和一次能力限制冲突的五集批次。

**结果：PASS**

- 正确选择 `review`。
- 使用10个维度和100分制，总分73/100，结论为“修订后复检”。
- 在第2集镜号5和第3集镜号5识别重复钩子。
- 在第4集镜号2和4识别违反“每夜一次”的能力限制。
- 每项评分均有集数或镜号证据。
- 修订建议限定在最小有效范围。

产物目录：`/tmp/manju-forward-review/`

### Review 回归测试

**目标：** 检查统一钩子分类、输入盲区、门槛依据和安全覆盖字段。

**结果：PASS**

- 使用统一钩子序列 `deadline → revelation`。
- 明确记录缺失 `outline.md` 及其评审盲区。
- 82/100按数字门槛原应“通过但可优化”，但严重安全问题通过独立覆盖字段将结果改为“修订后复检”。

产物目录：`/tmp/manju-forward-review-v2/`

## 4. 自定义参数覆盖回归

**请求：** 创建一部成年人悬疑漫剧，指定60集、每集90秒、每批6集。

**结果：PASS**

- `outline.md` 和 `catalog.md` 均恰好60行，编号1–60连续。
- 五幕范围派生为1–12、13–24、25–36、37–48、49–60。
- 10个批次范围从1–6连续派生至55–60。
- 项目进度分母为60集和10批。
- 生成文件未残留80集、120秒、每批5集、16批或默认幕范围。
- 因目录模式需要角色上游，评估者按规则补充 `characters.md`，未凭空跳过依赖。

产物目录：`/tmp/manju-forward-custom-v2/`

## 最终独立复审

最终评审重新检查设计与计划对齐、技能结构、渐进加载、资源链接、默认值及覆盖逻辑、依赖路由、安全边界、README、旧格式残留和用户创作文件边界。

- Critical：0
- Important：0
- Minor：0
- 结论：Ready
