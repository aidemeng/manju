# 《别慌，我真不是诡异始祖》

本目录是作品的新版项目根目录。

- `source-archive/`：迁移前原稿，只用于溯源和对照。
- `project.md`：项目参数与内容边界（待转换）。
- `outline.md`：故事总纲与连续性账本（待转换）。
- `characters.md`：角色圣经（待转换）。
- `catalog.md`：分集生产目录（待转换）。
- `episodes/`：新版分集分镜。
- `reviews/`：按批次生成的质检报告。

后续转换以 `source-archive/` 为输入，不直接改写归档原稿。
